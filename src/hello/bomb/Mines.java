package hello.bomb;

import java.util.Scanner;

/*
public class Mines {
    public static int mines(String[] args) {
        Scanner in = new Scanner(System.in);
        System.out.print("number of mines");
        int mines = in.nextInt();
        return mines;
    }
    public static int rows(String[] args) {
        Scanner in = new Scanner(System.in);
        System.out.print("number of rows");
        int input_rows = in.nextInt();
        return input_rows;
    }
    public static int columns(String[] args) {
        Scanner in = new Scanner(System.in);
        System.out.print("number of rows");
        int input_columns = in.nextInt();
        return input_columns;
    }
    public static return Array( ) {
        return int[][] myNumbers = { {input_rows}, {input_columns} };
    }
        for (int r = 0; r < input_row; r++)
        {
            for (int c = 0; c < input_row; c++ )
            {
                for ( int d = 0; d < mines; d ++)
            }
            for (int r = 0; r < input_row )
            {
                for (int c = 0; c < input_row; c++ )

}

    public static int [][] Board3 (){

        for (int r = 0; r < input_row; r++)
        {
            for (int c = 0; c < input_row; c++ )
            {
                for ( int d = 0; d < mines; d ++)
            }
            for (int r = 0; r < input_row )
            {
                for (int c = 0; c < input_row; c++ )
                {
                    for ( int d = 0; d < mines; d ++) {
                        int rrow = (int) (Math.random()*input_row);
                        int rcolumn = (int) (Math.random()*input_column);

                        return new int[0][];
                    }
*/